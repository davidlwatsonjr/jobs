<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Braintrust (<%= jobs.length %>)</title>
  </head>
  <body>
    <h1>Braintrust Job Links (<%= jobs.length %>)</h1>
    <ul>
      <% for (let i = 0; i < jobs.length; i++) { %>
      <li>
        <a href="<%= jobs[i].fullLink %>"
          ><%= jobs[i].title %> (<%= jobs[i].createdDate %>)</a
        >
      </li>
      <% } %>
    </ul>
  </body>
</html>
